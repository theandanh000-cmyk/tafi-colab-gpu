# TaFi Video Studio — Colab GPU Edition

1. Mở notebook: https://colab.research.google.com/github/  →  `tafi-colab-gpu.ipynb`
2. Runtime ▸ Run all (hoặc đăng nhập Google + dùng free GPU).
3. Copy link web in ra ở cell 7 (https://xxx.trycloudflare.com).

Cần zip `TaFi-VS-Tool-Web.zip` (cùng thư mục) — notebook tự đọc qua hộp thoại upload nếu không dùng GIT_URL.
